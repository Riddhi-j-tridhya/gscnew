jQuery(document).ready(function() {
            // Find the element with the old class and replace it with the new element
             $(".glyphicon.glyphicon-time").removeClass("glyphicon glyphicon-time").addClass("fa fa-solid fa-clock");
        })

 